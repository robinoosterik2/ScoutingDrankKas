FROM node:18

# Set workdir
WORKDIR /app
# Install deps
COPY app/package.json ./
RUN npm install

# Copy the rest of the app
COPY app .
ENV NODE_ENV=development

# Expose Nuxt default port
EXPOSE 3000