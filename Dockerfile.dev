FROM node:18

# Set workdir
WORKDIR /app
ENV NODE_OPTIONS="--max-old-space-size=2048"
# Install deps
COPY package*.json ./
RUN npm install

# Copy the rest of the app
COPY . .

# Expose Nuxt default port
EXPOSE 3000